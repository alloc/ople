import { app } from '../core'

app.command('build', 'Build the production server').action(() => {
  // TODO
  //  - rollup
  //  - adapter for firebase functions
  //  - deploy script for firebase/pushpin
})
